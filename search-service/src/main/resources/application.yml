spring:
  application:
    name: search-service
server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
grpc:
  client:
    song-service:
      address: song-service
      negotiation-type: plaintext