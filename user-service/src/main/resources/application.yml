spring:
  application:
    name: user-service
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: user-service
            client-secret: jSLIfcd5eq2t6e0CzNid3QKUaQNP1m0x
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/zing-mp3
server:
  port: 8083
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

springdoc:
  swagger-ui:
    oauth:
      client-id: ${keycloak.client-id}
      client-secret: ${keycloak.client-secret}
    operations-sorter: method
    tags-sorter: alpha


realm: zing-mp3
server-url: http://localhost:8080
client-id: admin-cli
grant-type: password
name: admin
password: Demo@123


client-id-user-service: user-service
