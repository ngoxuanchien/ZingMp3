spring:
  application:
    name: user-service
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: user-service
            client-secret: jSLIfcd5eq2t6e0CzNid3QKUaQNP1m0x
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/zing-mp3
server:
  port: 8083
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
keycloak:
  domain: localhost
  realm: zing-mp3
  client-id: user-service
  client-secret: jSLIfcd5eq2t6e0CzNid3QKUaQNP1m0x
  urls:
    auth: http://${keycloak.domain}:8080
  token-url: http://localhost:8081/api/auth/token
  resource-id: user-service

springdoc:
  swagger-ui:
    oauth:
      client-id: ${keycloak.client-id}
      client-secret: ${keycloak.client-secret}
    operations-sorter: method
    tags-sorter: alpha
